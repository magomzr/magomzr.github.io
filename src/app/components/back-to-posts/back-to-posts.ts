import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-back-to-posts',
  imports: [RouterLink],
  templateUrl: './back-to-posts.html',
  styleUrl: './back-to-posts.css',
})
export class BackToPosts {}
